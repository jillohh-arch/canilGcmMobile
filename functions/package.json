{
  "name": "canil-gcm-functions",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test lib/test-build/test/admin_patch_k9_identity.test.js lib/test-build/test/admin_create_human.test.js",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.4.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
